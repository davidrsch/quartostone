node_modules/
dist/
_site/
.quartostone/
*.quartostone.lock
.DS_Store
Thumbs.db
*.log
# Test output
coverage/
playwright-report/
test-results/
# E2E fixture workspace git repo (initialised at runtime, not tracked)
tests/e2e/fixtures/workspace/.git/
.env
# panmirror.js is a vendored UMD bundle built from quarto-fork.
# It is committed so that CI can run the visual-editor round-trip E2E tests.
# To rebuild locally: npm run build:panmirror
